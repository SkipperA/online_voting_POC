{
  "name": "ovpoc-voter-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The voter's application. Blinding runs here, in the browser.",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "esbuild src/main.ts --bundle --format=iife --platform=browser --target=es2022 --outfile=static/bundle.js",
    "watch": "npm run build -- --watch"
  },
  "dependencies": {
    "@cloudflare/blindrsa-ts": "0.4.4"
  },
  "devDependencies": {
    "esbuild": "0.25.10"
  }
}
